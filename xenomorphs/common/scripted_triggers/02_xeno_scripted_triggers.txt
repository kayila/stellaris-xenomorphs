# Add Xenomorphs to the types of critters considered an organic species. This allows them to be assembled with organic pop assembly.

is_organic_species = {
	species = {
		OR = {
			is_archetype = BIOLOGICAL # original trigger did not include pre-sapient classes
			is_archetype = LITHOID
			is_archetype = XENO # Add the xenomorph archtype
		}
	}
}